# Docker Compose / KrakenD JWT validation. These values are public metadata,
# but the local root .env remains ignored so environments can differ safely.
CLERK_ISSUER=https://example.clerk.accounts.dev
CLERK_JWKS_URL=https://example.clerk.accounts.dev/.well-known/jwks.json
