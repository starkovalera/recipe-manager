Use $product-ui-ux-design and $frontend-design.

After the user selects a low-fidelity direction, build an isolated browser prototype under:

`design/recipe-detail/prototypes/default-view/`

Requirements:
- local mock data only;
- no imports from `frontend/src`;
- no application API calls;
- selectable normal, dense, no-cover, manual, and flagged states;
- selectable approved metadata variant;
- desktop and mobile layouts;
- clear startup instructions.

Preserve approved structure exactly.

Do not implement production functionality.
