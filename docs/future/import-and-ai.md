# Import and AI Evolution

Stage: Captured
First-version scope: excluded unless separately promoted

## Opportunities

- remove legacy `coverCandidate` compatibility fields after confirming no consumer remains;
- generated covers with approved quality, cost, provenance, and lifecycle rules;
- align extraction prompts with backend-owned content limits;
- require AI tags to use supplied allowlist values verbatim;
- distinguish source-authored recommendations from generated notes;
- preserve supported-platform author profile links separately from display names;
- Telegram import;
- Instagram Reels and short-video hardening;
- explicit supported-platform URL validation and user-facing unsupported-platform errors;
- silent-video versus transcription-failure classification;
- environment-backed maximum video duration;
- review behavior when a secondary URL produces no usable evidence.

## Refinement boundary

Split this list into independent capabilities before promotion. Each promoted capability must define product value, provider and policy constraints, source/resource semantics, failure behavior, observability, and the affected Design and Development tracks.
