# Product Expansion

Stage: Captured
First-version scope: excluded unless separately promoted

## Opportunities

- mandatory first-login language onboarding and localized default tags;
- configurable per-user successful-import quotas and quota tiers;
- recipe ratings and rating comments;
- editable recipe difficulty, including import extraction support;
- shared recipe libraries with explicit membership and ownership;
- backend-owned editable-field capability limits;
- additional recipe image uploads and lifecycle behavior;
- locale-aware title normalization;
- fully manual recipes and standalone collection notes;
- owner-scoped Author entities, author links, selection, pages, and merge workflows.

## Refinement rule

Treat each opportunity as its own Future Capability before promotion. Resolve ownership, visibility, lifecycle, permissions, migration, search/indexing effects, web/mobile journeys, and API boundaries before generating issues.
